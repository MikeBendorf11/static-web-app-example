{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "scripts": { 
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "playwright_test" : "playwright test",
    "test": "swa start src --api-location api"
  },
  "devDependencies": {
    "@playwright/test": "^1.22.2"
  }
}
